#include<stdio.h>
#include<stdlib.h>
{
    struct node
    {
        int data;
        struct node *next;
    }
    struct node *head,*ptr;
    ptr = (struct node*)malloc(sizeof(stuct node*));

    
}
